package interfaces;

interface Bicycle {
//	interfaces define the specification of how a class would act or
//	interfaces are defining contracts or specification of how a code,app or programming language would behave
//	a concrete method cannot be defined in an interface but can be declared 

	void applybreak(int decrement);

	void speedup(int increment);

}
